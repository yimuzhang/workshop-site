services:
  web:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: workshop-web

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

    environment:
      WATCHPACK_POLLING: "true"

    command: npm run dev